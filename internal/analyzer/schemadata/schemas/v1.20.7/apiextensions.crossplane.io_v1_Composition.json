{
  "release": "v1.20.7",
  "apiVersion": "apiextensions.crossplane.io/v1",
  "kind": "Composition",
  "fields": [
    {
      "path": "apiVersion",
      "description": "API version of the Composition resource.",
      "type": "string"
    },
    {
      "path": "kind",
      "description": "Resource kind, normally Composition.",
      "type": "string"
    },
    {
      "path": "metadata",
      "type": "object"
    },
    {
      "path": "metadata.annotations",
      "description": "Object annotations.",
      "type": "object"
    },
    {
      "path": "metadata.labels",
      "description": "Object labels.",
      "type": "object"
    },
    {
      "path": "metadata.name",
      "description": "Name of the Composition.",
      "type": "string"
    },
    {
      "path": "spec",
      "description": "CompositionSpec specifies desired state of a composition.",
      "type": "object"
    },
    {
      "path": "spec.compositeTypeRef",
      "description": "CompositeTypeRef specifies the type of composite resource that this\ncomposition is compatible with.",
      "type": "object",
      "required": true
    },
    {
      "path": "spec.compositeTypeRef.apiVersion",
      "description": "API version of the composite resource type this Composition renders.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.compositeTypeRef.kind",
      "description": "Kind of the composite resource type this Composition renders.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.mode",
      "description": "Mode controls what type or \"mode\" of Composition will be used.\n\n\"Pipeline\" indicates that a Composition specifies a pipeline of\nComposition Functions, each of which is responsible for producing\ncomposed resources that Crossplane should create or update.\n\n\"Resources\" indicates that a Composition uses what is commonly referred\nto as \"Patch \u0026 Transform\" or P\u0026T composition. This mode of Composition\nuses an array of resources, each a template for a composed resource.\n\nAll Compositions should use Pipeline mode. Resources mode is deprecated.\nResources mode won't be removed in Crossplane 1.x, and will remain the\ndefault to avoid breaking legacy Compositions. However, it's no longer\naccepting new features, and only accepting security related bug fixes.",
      "type": "string",
      "default": "Resources",
      "enum": [
        "Resources",
        "Pipeline"
      ]
    },
    {
      "path": "spec.patchSets[]",
      "description": "PatchSets define a named set of patches that may be included by any\nresource in this Composition. PatchSets cannot themselves refer to other\nPatchSets.\n\nPatchSets are only used by the \"Resources\" mode of Composition. They\nare ignored by other modes.\n\nDeprecated: Use Composition Functions instead.",
      "type": "array"
    },
    {
      "path": "spec.patchSets[].name",
      "description": "Name of this PatchSet.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[]",
      "description": "Patches will be applied as an overlay to the base resource.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].combine",
      "description": "Combine is the patch configuration for a CombineFromComposite or\nCombineToComposite patch.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].combine.strategy",
      "description": "Strategy defines the strategy to use to combine the input variable values.\nCurrently only string is supported.",
      "type": "string",
      "required": true,
      "enum": [
        "string"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].combine.string",
      "description": "String declares that input variables should be combined into a single\nstring, using the relevant settings for formatting purposes.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].combine.string.fmt",
      "description": "Format the input using a Go format string. See\nhttps://golang.org/pkg/fmt/ for details.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].combine.variables[]",
      "description": "Variables are the list of variables whose values will be retrieved and\ncombined.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].combine.variables[].fromFieldPath",
      "description": "FromFieldPath is the path of the field on the source whose value is\nto be used as input.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].fromFieldPath",
      "description": "FromFieldPath is the path of the field on the resource whose value is\nto be used as input. Required when type is FromCompositeFieldPath or\nToCompositeFieldPath.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].patchSetName",
      "description": "PatchSetName to include patches from. Required when type is PatchSet.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].policy",
      "description": "Policy configures the specifics of patching behaviour.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].policy.fromFieldPath",
      "description": "FromFieldPath specifies how to patch from a field path. The default is\n'Optional', which means the patch will be a no-op if the specified\nfromFieldPath does not exist. Use 'Required' if the patch should fail if\nthe specified path does not exist.",
      "type": "string",
      "enum": [
        "Optional",
        "Required"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].policy.mergeOptions",
      "description": "MergeOptions Specifies merge options on a field path.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].policy.mergeOptions.appendSlice",
      "description": "Specifies that already existing elements in a merged slice should be preserved",
      "type": "boolean"
    },
    {
      "path": "spec.patchSets[].patches[].policy.mergeOptions.keepMapValues",
      "description": "Specifies that already existing values in a merged map should be preserved",
      "type": "boolean"
    },
    {
      "path": "spec.patchSets[].patches[].toFieldPath",
      "description": "ToFieldPath is the path of the field on the resource whose value will\nbe changed with the result of transforms. Leave empty if you'd like to\npropagate to the same path as fromFieldPath.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[]",
      "description": "Transforms are the list of functions that are used as a FIFO pipe for the\ninput to be transformed.",
      "type": "array"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].convert",
      "description": "Convert is used to cast the input into the given output type.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].convert.format",
      "description": "The expected input format.\n\n* `quantity` - parses the input as a K8s [`resource.Quantity`](https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity).\nOnly used during `string -\u003e float64` conversions.\n* `json` - parses the input as a JSON string.\nOnly used during `string -\u003e object` or `string -\u003e list` conversions.\n\nIf this property is null, the default conversion is applied.",
      "type": "string",
      "enum": [
        "none",
        "quantity",
        "json"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].convert.toType",
      "description": "ToType is the type of the output of this transform.",
      "type": "string",
      "required": true,
      "enum": [
        "string",
        "int",
        "int64",
        "bool",
        "float64",
        "object",
        "array"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].map",
      "description": "Map uses the input as a key in the given map and returns the value.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match",
      "description": "Match is a more complex version of Map that matches a list of patterns.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.fallbackTo",
      "description": "Determines to what value the transform should fallback if no pattern matches.",
      "type": "string",
      "default": "Value",
      "enum": [
        "Value",
        "Input"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.fallbackValue",
      "description": "The fallback value that should be returned by the transform if now pattern\nmatches."
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.patterns[]",
      "description": "The patterns that should be tested against the input string.\nPatterns are tested in order. The value of the first match is used as\nresult of this transform.",
      "type": "array"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.patterns[].literal",
      "description": "Literal exactly matches the input string (case sensitive).\nIs required if `type` is `literal`.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.patterns[].regexp",
      "description": "Regexp to match against the input string.\nIs required if `type` is `regexp`.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.patterns[].result",
      "description": "The value that is used as result of the transform if the pattern matches.",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].match.patterns[].type",
      "description": "Type specifies how the pattern matches the input.\n\n* `literal` - the pattern value has to exactly match (case sensitive) the\ninput string. This is the default.\n\n* `regexp` - the pattern treated as a regular expression against\nwhich the input string is tested. Crossplane will throw an error if the\nkey is not a valid regexp.",
      "type": "string",
      "required": true,
      "default": "literal",
      "enum": [
        "literal",
        "regexp"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].math",
      "description": "Math is used to transform the input via mathematical operations such as\nmultiplication.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].math.clampMax",
      "description": "ClampMax makes sure that the value is not bigger than the given value.",
      "type": "integer"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].math.clampMin",
      "description": "ClampMin makes sure that the value is not smaller than the given value.",
      "type": "integer"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].math.multiply",
      "description": "Multiply the value.",
      "type": "integer"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].math.type",
      "description": "Type of the math transform to be run.",
      "type": "string",
      "default": "Multiply",
      "enum": [
        "Multiply",
        "ClampMin",
        "ClampMax"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string",
      "description": "String is used to transform the input into a string or a different kind\nof string. Note that the input does not necessarily need to be a string.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.convert",
      "description": "Optional conversion method to be specified.\n`ToUpper` and `ToLower` change the letter case of the input string.\n`ToBase64` and `FromBase64` perform a base64 conversion based on the input string.\n`ToJson` converts any input value into its raw JSON representation.\n`ToSha1`, `ToSha256` and `ToSha512` generate a hash value based on the input\nconverted to JSON.\n`ToAdler32` generate a addler32 hash based on the input string.",
      "type": "string",
      "enum": [
        "ToUpper",
        "ToLower",
        "ToBase64",
        "FromBase64",
        "ToJson",
        "ToSha1",
        "ToSha256",
        "ToSha512",
        "ToAdler32"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.fmt",
      "description": "Format the input using a Go format string. See\nhttps://golang.org/pkg/fmt/ for details.",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.join",
      "description": "Join defines parameters to join a slice of values to a string.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.join.separator",
      "description": "Separator defines the character that should separate the values from each\nother in the joined string.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.regexp",
      "description": "Extract a match from the input using a regular expression.",
      "type": "object"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.regexp.group",
      "description": "Group number to match. 0 (the default) matches the entire expression.",
      "type": "integer"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.regexp.match",
      "description": "Match string. May optionally include submatches, aka capture groups.\nSee https://pkg.go.dev/regexp/ for details.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.trim",
      "description": "Trim the prefix or suffix from the input",
      "type": "string"
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].string.type",
      "description": "Type of the string transform to be run.",
      "type": "string",
      "default": "Format",
      "enum": [
        "Format",
        "Convert",
        "TrimPrefix",
        "TrimSuffix",
        "Regexp",
        "Join"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].transforms[].type",
      "description": "Type of the transform to be run.",
      "type": "string",
      "required": true,
      "enum": [
        "map",
        "match",
        "math",
        "string",
        "convert"
      ]
    },
    {
      "path": "spec.patchSets[].patches[].type",
      "description": "Type sets the patching behaviour to be used. Each patch type may require\nits own fields to be set on the Patch object.",
      "type": "string",
      "default": "FromCompositeFieldPath",
      "enum": [
        "FromCompositeFieldPath",
        "PatchSet",
        "ToCompositeFieldPath",
        "CombineFromComposite",
        "CombineToComposite"
      ]
    },
    {
      "path": "spec.pipeline[]",
      "description": "Pipeline is a list of composition function steps that will be used when a\ncomposite resource referring to this composition is created. One of\nresources and pipeline must be specified - you cannot specify both.\n\nThe Pipeline is only used by the \"Pipeline\" mode of Composition. It is\nignored by other modes.",
      "type": "array"
    },
    {
      "path": "spec.pipeline[].credentials[]",
      "description": "Credentials are optional credentials that the Composition Function needs.",
      "type": "array"
    },
    {
      "path": "spec.pipeline[].credentials[].name",
      "description": "Name of this set of credentials.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.pipeline[].credentials[].secretRef",
      "description": "A SecretRef is a reference to a secret containing credentials that should\nbe supplied to the function.",
      "type": "object"
    },
    {
      "path": "spec.pipeline[].credentials[].secretRef.name",
      "description": "Name of the secret.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.pipeline[].credentials[].secretRef.namespace",
      "description": "Namespace of the secret.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.pipeline[].credentials[].source",
      "description": "Source of the function credentials.",
      "type": "string",
      "required": true,
      "enum": [
        "None",
        "Secret"
      ]
    },
    {
      "path": "spec.pipeline[].functionRef",
      "description": "FunctionRef is a reference to the Composition Function this step should\nexecute.",
      "type": "object",
      "required": true
    },
    {
      "path": "spec.pipeline[].functionRef.name",
      "description": "Name of the referenced Function.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.pipeline[].input",
      "description": "Input is an optional, arbitrary Kubernetes resource (i.e. a resource\nwith an apiVersion and kind) that will be passed to the Composition\nFunction as the 'input' of its RunFunctionRequest.",
      "type": "object"
    },
    {
      "path": "spec.pipeline[].step",
      "description": "Step name. Must be unique within its Pipeline.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.publishConnectionDetailsWithStoreConfigRef",
      "description": "PublishConnectionDetailsWithStoreConfig specifies the secret store config\nwith which the connection details of composite resources dynamically\nprovisioned using this composition will be published.\n\nTHIS IS AN ALPHA FIELD. Do not use it in production. It is not honored\nunless the relevant Crossplane feature flag is enabled, and may be\nchanged or removed without notice.",
      "type": "object",
      "default": {
        "name": "default"
      }
    },
    {
      "path": "spec.publishConnectionDetailsWithStoreConfigRef.name",
      "description": "Name of the referenced StoreConfig.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.resources[]",
      "description": "Resources is a list of resource templates that will be used when a\ncomposite resource referring to this composition is created.\n\nResources are only used by the \"Resources\" mode of Composition. They are\nignored by other modes.\n\nDeprecated: Use Composition Functions instead.",
      "type": "array"
    },
    {
      "path": "spec.resources[].base",
      "description": "Base is the target resource that the patches will be applied on.",
      "type": "object",
      "required": true
    },
    {
      "path": "spec.resources[].connectionDetails[]",
      "description": "ConnectionDetails lists the propagation secret keys from this target\nresource to the composition instance connection secret.",
      "type": "array"
    },
    {
      "path": "spec.resources[].connectionDetails[].fromConnectionSecretKey",
      "description": "FromConnectionSecretKey is the key that will be used to fetch the value\nfrom the composed resource's connection secret.",
      "type": "string"
    },
    {
      "path": "spec.resources[].connectionDetails[].fromFieldPath",
      "description": "FromFieldPath is the path of the field on the composed resource whose\nvalue to be used as input. Name must be specified if the type is\nFromFieldPath.",
      "type": "string"
    },
    {
      "path": "spec.resources[].connectionDetails[].name",
      "description": "Name of the connection secret key that will be propagated to the\nconnection secret of the composition instance. Leave empty if you'd like\nto use the same key name.",
      "type": "string"
    },
    {
      "path": "spec.resources[].connectionDetails[].type",
      "description": "Type sets the connection detail fetching behaviour to be used. Each\nconnection detail type may require its own fields to be set on the\nConnectionDetail object. If the type is omitted Crossplane will attempt\nto infer it based on which other fields were specified. If multiple\nfields are specified the order of precedence is:\n1. FromValue\n2. FromConnectionSecretKey\n3. FromFieldPath",
      "type": "string",
      "enum": [
        "FromConnectionSecretKey",
        "FromFieldPath",
        "FromValue"
      ]
    },
    {
      "path": "spec.resources[].connectionDetails[].value",
      "description": "Value that will be propagated to the connection secret of the composite\nresource. May be set to inject a fixed, non-sensitive connection secret\nvalue, for example a well-known port.",
      "type": "string"
    },
    {
      "path": "spec.resources[].name",
      "description": "A Name uniquely identifies this entry within its Composition's resources\narray. Names are optional but *strongly* recommended. When all entries in\nthe resources array are named entries may added, deleted, and reordered\nas long as their names do not change. When entries are not named the\nlength and order of the resources array should be treated as immutable.\nEither all or no entries must be named.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[]",
      "description": "Patches will be applied as overlay to the base resource.",
      "type": "array"
    },
    {
      "path": "spec.resources[].patches[].combine",
      "description": "Combine is the patch configuration for a CombineFromComposite or\nCombineToComposite patch.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].combine.strategy",
      "description": "Strategy defines the strategy to use to combine the input variable values.\nCurrently only string is supported.",
      "type": "string",
      "required": true,
      "enum": [
        "string"
      ]
    },
    {
      "path": "spec.resources[].patches[].combine.string",
      "description": "String declares that input variables should be combined into a single\nstring, using the relevant settings for formatting purposes.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].combine.string.fmt",
      "description": "Format the input using a Go format string. See\nhttps://golang.org/pkg/fmt/ for details.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].combine.variables[]",
      "description": "Variables are the list of variables whose values will be retrieved and\ncombined.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].combine.variables[].fromFieldPath",
      "description": "FromFieldPath is the path of the field on the source whose value is\nto be used as input.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].fromFieldPath",
      "description": "FromFieldPath is the path of the field on the resource whose value is\nto be used as input. Required when type is FromCompositeFieldPath or\nToCompositeFieldPath.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].patchSetName",
      "description": "PatchSetName to include patches from. Required when type is PatchSet.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].policy",
      "description": "Policy configures the specifics of patching behaviour.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].policy.fromFieldPath",
      "description": "FromFieldPath specifies how to patch from a field path. The default is\n'Optional', which means the patch will be a no-op if the specified\nfromFieldPath does not exist. Use 'Required' if the patch should fail if\nthe specified path does not exist.",
      "type": "string",
      "enum": [
        "Optional",
        "Required"
      ]
    },
    {
      "path": "spec.resources[].patches[].policy.mergeOptions",
      "description": "MergeOptions Specifies merge options on a field path.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].policy.mergeOptions.appendSlice",
      "description": "Specifies that already existing elements in a merged slice should be preserved",
      "type": "boolean"
    },
    {
      "path": "spec.resources[].patches[].policy.mergeOptions.keepMapValues",
      "description": "Specifies that already existing values in a merged map should be preserved",
      "type": "boolean"
    },
    {
      "path": "spec.resources[].patches[].toFieldPath",
      "description": "ToFieldPath is the path of the field on the resource whose value will\nbe changed with the result of transforms. Leave empty if you'd like to\npropagate to the same path as fromFieldPath.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].transforms[]",
      "description": "Transforms are the list of functions that are used as a FIFO pipe for the\ninput to be transformed.",
      "type": "array"
    },
    {
      "path": "spec.resources[].patches[].transforms[].convert",
      "description": "Convert is used to cast the input into the given output type.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].convert.format",
      "description": "The expected input format.\n\n* `quantity` - parses the input as a K8s [`resource.Quantity`](https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity).\nOnly used during `string -\u003e float64` conversions.\n* `json` - parses the input as a JSON string.\nOnly used during `string -\u003e object` or `string -\u003e list` conversions.\n\nIf this property is null, the default conversion is applied.",
      "type": "string",
      "enum": [
        "none",
        "quantity",
        "json"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].convert.toType",
      "description": "ToType is the type of the output of this transform.",
      "type": "string",
      "required": true,
      "enum": [
        "string",
        "int",
        "int64",
        "bool",
        "float64",
        "object",
        "array"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].map",
      "description": "Map uses the input as a key in the given map and returns the value.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].match",
      "description": "Match is a more complex version of Map that matches a list of patterns.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.fallbackTo",
      "description": "Determines to what value the transform should fallback if no pattern matches.",
      "type": "string",
      "default": "Value",
      "enum": [
        "Value",
        "Input"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.fallbackValue",
      "description": "The fallback value that should be returned by the transform if now pattern\nmatches."
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.patterns[]",
      "description": "The patterns that should be tested against the input string.\nPatterns are tested in order. The value of the first match is used as\nresult of this transform.",
      "type": "array"
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.patterns[].literal",
      "description": "Literal exactly matches the input string (case sensitive).\nIs required if `type` is `literal`.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.patterns[].regexp",
      "description": "Regexp to match against the input string.\nIs required if `type` is `regexp`.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.patterns[].result",
      "description": "The value that is used as result of the transform if the pattern matches.",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].transforms[].match.patterns[].type",
      "description": "Type specifies how the pattern matches the input.\n\n* `literal` - the pattern value has to exactly match (case sensitive) the\ninput string. This is the default.\n\n* `regexp` - the pattern treated as a regular expression against\nwhich the input string is tested. Crossplane will throw an error if the\nkey is not a valid regexp.",
      "type": "string",
      "required": true,
      "default": "literal",
      "enum": [
        "literal",
        "regexp"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].math",
      "description": "Math is used to transform the input via mathematical operations such as\nmultiplication.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].math.clampMax",
      "description": "ClampMax makes sure that the value is not bigger than the given value.",
      "type": "integer"
    },
    {
      "path": "spec.resources[].patches[].transforms[].math.clampMin",
      "description": "ClampMin makes sure that the value is not smaller than the given value.",
      "type": "integer"
    },
    {
      "path": "spec.resources[].patches[].transforms[].math.multiply",
      "description": "Multiply the value.",
      "type": "integer"
    },
    {
      "path": "spec.resources[].patches[].transforms[].math.type",
      "description": "Type of the math transform to be run.",
      "type": "string",
      "default": "Multiply",
      "enum": [
        "Multiply",
        "ClampMin",
        "ClampMax"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].string",
      "description": "String is used to transform the input into a string or a different kind\nof string. Note that the input does not necessarily need to be a string.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.convert",
      "description": "Optional conversion method to be specified.\n`ToUpper` and `ToLower` change the letter case of the input string.\n`ToBase64` and `FromBase64` perform a base64 conversion based on the input string.\n`ToJson` converts any input value into its raw JSON representation.\n`ToSha1`, `ToSha256` and `ToSha512` generate a hash value based on the input\nconverted to JSON.\n`ToAdler32` generate a addler32 hash based on the input string.",
      "type": "string",
      "enum": [
        "ToUpper",
        "ToLower",
        "ToBase64",
        "FromBase64",
        "ToJson",
        "ToSha1",
        "ToSha256",
        "ToSha512",
        "ToAdler32"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.fmt",
      "description": "Format the input using a Go format string. See\nhttps://golang.org/pkg/fmt/ for details.",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.join",
      "description": "Join defines parameters to join a slice of values to a string.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.join.separator",
      "description": "Separator defines the character that should separate the values from each\nother in the joined string.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.regexp",
      "description": "Extract a match from the input using a regular expression.",
      "type": "object"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.regexp.group",
      "description": "Group number to match. 0 (the default) matches the entire expression.",
      "type": "integer"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.regexp.match",
      "description": "Match string. May optionally include submatches, aka capture groups.\nSee https://pkg.go.dev/regexp/ for details.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.trim",
      "description": "Trim the prefix or suffix from the input",
      "type": "string"
    },
    {
      "path": "spec.resources[].patches[].transforms[].string.type",
      "description": "Type of the string transform to be run.",
      "type": "string",
      "default": "Format",
      "enum": [
        "Format",
        "Convert",
        "TrimPrefix",
        "TrimSuffix",
        "Regexp",
        "Join"
      ]
    },
    {
      "path": "spec.resources[].patches[].transforms[].type",
      "description": "Type of the transform to be run.",
      "type": "string",
      "required": true,
      "enum": [
        "map",
        "match",
        "math",
        "string",
        "convert"
      ]
    },
    {
      "path": "spec.resources[].patches[].type",
      "description": "Type sets the patching behaviour to be used. Each patch type may require\nits own fields to be set on the Patch object.",
      "type": "string",
      "default": "FromCompositeFieldPath",
      "enum": [
        "FromCompositeFieldPath",
        "PatchSet",
        "ToCompositeFieldPath",
        "CombineFromComposite",
        "CombineToComposite"
      ]
    },
    {
      "path": "spec.resources[].readinessChecks[]",
      "description": "ReadinessChecks allows users to define custom readiness checks. All checks\nhave to return true in order for resource to be considered ready. The\ndefault readiness check is to have the \"Ready\" condition to be \"True\".",
      "type": "array",
      "default": [
        {
          "matchCondition": {
            "status": "True",
            "type": "Ready"
          },
          "type": "MatchCondition"
        }
      ]
    },
    {
      "path": "spec.resources[].readinessChecks[].fieldPath",
      "description": "FieldPath shows the path of the field whose value will be used.",
      "type": "string"
    },
    {
      "path": "spec.resources[].readinessChecks[].matchCondition",
      "description": "MatchCondition specifies the condition you'd like to match if you're using \"MatchCondition\" type.",
      "type": "object"
    },
    {
      "path": "spec.resources[].readinessChecks[].matchCondition.status",
      "description": "Status is the status of the condition you'd like to match.",
      "type": "string",
      "required": true,
      "default": "True"
    },
    {
      "path": "spec.resources[].readinessChecks[].matchCondition.type",
      "description": "Type indicates the type of condition you'd like to use.",
      "type": "string",
      "required": true,
      "default": "Ready"
    },
    {
      "path": "spec.resources[].readinessChecks[].matchInteger",
      "description": "MatchInt is the value you'd like to match if you're using \"MatchInt\" type.",
      "type": "integer"
    },
    {
      "path": "spec.resources[].readinessChecks[].matchString",
      "description": "MatchString is the value you'd like to match if you're using \"MatchString\" type.",
      "type": "string"
    },
    {
      "path": "spec.resources[].readinessChecks[].type",
      "description": "Type indicates the type of probe you'd like to use.",
      "type": "string",
      "required": true,
      "enum": [
        "MatchString",
        "MatchInteger",
        "NonEmpty",
        "MatchCondition",
        "MatchTrue",
        "MatchFalse",
        "None"
      ]
    },
    {
      "path": "spec.writeConnectionSecretsToNamespace",
      "description": "WriteConnectionSecretsToNamespace specifies the namespace in which the\nconnection secrets of composite resource dynamically provisioned using\nthis composition will be created.\nThis field is planned to be replaced in a future release in favor of\nPublishConnectionDetailsWithStoreConfigRef. Currently, both could be\nset independently and connection details would be published to both\nwithout affecting each other as long as related fields at MR level\nspecified.",
      "type": "string"
    }
  ],
  "provenance": {
    "owner": "core",
    "source": "generated-built-in",
    "upstreamReleaseTag": "v1.20.7",
    "upstreamSourcePath": "apiextensions.crossplane.io_compositions.yaml",
    "upstreamSHA256": "bf05e4ba6b676d95ade3b122ed9d2004f3d681b62b306874ad743cdc0fac0d6f"
  }
}

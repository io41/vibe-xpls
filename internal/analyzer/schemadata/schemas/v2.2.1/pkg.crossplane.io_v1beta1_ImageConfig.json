{
  "release": "v2.2.1",
  "apiVersion": "pkg.crossplane.io/v1beta1",
  "kind": "ImageConfig",
  "fields": [
    {
      "path": "apiVersion",
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    {
      "path": "kind",
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    {
      "path": "metadata",
      "type": "object"
    },
    {
      "path": "metadata.annotations",
      "description": "Object annotations.",
      "type": "object"
    },
    {
      "path": "metadata.labels",
      "description": "Object labels.",
      "type": "object"
    },
    {
      "path": "metadata.name",
      "description": "Object name.",
      "type": "string"
    },
    {
      "path": "spec",
      "description": "ImageConfigSpec contains the configuration for matching images.",
      "type": "object"
    },
    {
      "path": "spec.matchImages[]",
      "description": "MatchImages is a list of image matching rules. This ImageConfig will\nmatch an image if any one of these rules is satisfied. In the case where\nmultiple ImageConfigs match an image for a given purpose the one with the\nmost specific match will be used. If multiple rules of equal specificity\nmatch an arbitrary one will be selected.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.matchImages[].prefix",
      "description": "Prefix is the prefix that should be matched. When multiple prefix rules\nmatch an image path, the longest one takes precedence.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.matchImages[].type",
      "description": "Type is the type of match.",
      "type": "string",
      "default": "Prefix",
      "enum": [
        "Prefix"
      ]
    },
    {
      "path": "spec.registry",
      "description": "Registry is the configuration for the registry.",
      "type": "object"
    },
    {
      "path": "spec.registry.authentication",
      "description": "Authentication is the authentication information for the registry.",
      "type": "object"
    },
    {
      "path": "spec.registry.authentication.pullSecretRef",
      "description": "PullSecretRef is a reference to a secret that contains the credentials for\nthe registry.",
      "type": "object",
      "required": true
    },
    {
      "path": "spec.registry.authentication.pullSecretRef.name",
      "description": "Name of the referent.\nThis field is effectively required, but due to backwards compatibility is\nallowed to be empty. Instances of this type with an empty value here are\nalmost certainly wrong.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names",
      "type": "string",
      "default": ""
    },
    {
      "path": "spec.rewriteImage",
      "description": "RewriteImage defines how a matched image's path should be rewritten.",
      "type": "object"
    },
    {
      "path": "spec.rewriteImage.prefix",
      "description": "Prefix is the prefix that will replace the portion of the image's path\nmatched by the prefix in the ImageMatch. If multiple prefixes matched,\nthe longest one will be replaced.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.runtime",
      "description": "Runtime allows configuration of runtime options for the image.",
      "type": "object"
    },
    {
      "path": "spec.runtime.configRef",
      "description": "ConfigReference references a RuntimeConfig resource that will be\nused to configure the package runtime.",
      "type": "object"
    },
    {
      "path": "spec.runtime.configRef.apiVersion",
      "description": "API version of the referent.",
      "type": "string",
      "default": "pkg.crossplane.io/v1beta1"
    },
    {
      "path": "spec.runtime.configRef.kind",
      "description": "Kind of the referent.",
      "type": "string",
      "default": "DeploymentRuntimeConfig"
    },
    {
      "path": "spec.runtime.configRef.name",
      "description": "Name of the RuntimeConfig.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification",
      "description": "Verification contains the configuration for verifying the image.",
      "type": "object"
    },
    {
      "path": "spec.verification.cosign",
      "description": "Cosign is the configuration for verifying the image using cosign.",
      "type": "object"
    },
    {
      "path": "spec.verification.cosign.authorities[]",
      "description": "Authorities defines the rules for discovering and validating signatures.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].attestations[]",
      "description": "Attestations is a list of individual attestations for this authority,\nonce the signature for this authority has been verified.",
      "type": "array"
    },
    {
      "path": "spec.verification.cosign.authorities[].attestations[].name",
      "description": "Name of the attestation.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].attestations[].predicateType",
      "description": "PredicateType defines which predicate type to verify. Matches cosign\nverify-attestation options.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].key",
      "description": "Key defines the type of key to validate the image.",
      "type": "object"
    },
    {
      "path": "spec.verification.cosign.authorities[].key.hashAlgorithm",
      "description": "HashAlgorithm always defaults to sha256 if the algorithm hasn't been explicitly set",
      "type": "string",
      "required": true,
      "default": "sha256"
    },
    {
      "path": "spec.verification.cosign.authorities[].key.secretRef",
      "description": "SecretRef sets a reference to a secret with the key.",
      "type": "object",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].key.secretRef.key",
      "description": "The key to select.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].key.secretRef.name",
      "description": "Name of the secret.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless",
      "description": "Keyless sets the configuration to verify the authority against a Fulcio\ninstance.",
      "type": "object"
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.identities[]",
      "description": "Identities sets a list of identities.",
      "type": "array",
      "required": true
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.identities[].issuer",
      "description": "Issuer defines the issuer for this identity.",
      "type": "string"
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.identities[].issuerRegExp",
      "description": "IssuerRegExp specifies a regular expression to match the issuer for this identity.\nThis has precedence over the Issuer field.",
      "type": "string"
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.identities[].subject",
      "description": "Subject defines the subject for this identity.",
      "type": "string"
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.identities[].subjectRegExp",
      "description": "SubjectRegExp specifies a regular expression to match the subject for this identity.\nThis has precedence over the Subject field.",
      "type": "string"
    },
    {
      "path": "spec.verification.cosign.authorities[].keyless.insecureIgnoreSCT",
      "description": "InsecureIgnoreSCT omits verifying if a certificate contains an embedded SCT",
      "type": "boolean"
    },
    {
      "path": "spec.verification.cosign.authorities[].name",
      "description": "Name is the name for this authority.",
      "type": "string",
      "required": true
    },
    {
      "path": "spec.verification.provider",
      "description": "Provider is the provider that should be used to verify the image.",
      "type": "string",
      "required": true,
      "enum": [
        "Cosign"
      ]
    }
  ],
  "provenance": {
    "owner": "core",
    "source": "generated-built-in",
    "upstreamReleaseTag": "v2.2.1",
    "upstreamSourcePath": "pkg.crossplane.io_imageconfigs.yaml",
    "upstreamSHA256": "81580527c9229e663cc055ec50bdc2e98911d097d098078ad6c46cb2126c3f22"
  }
}

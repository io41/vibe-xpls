# xpls:doc field=spec.compositeTypeRef.apiVersion text=Composite API version selected by this Composition.
# xpls:doc field=spec.pipeline.step text=Function pipeline step identifier used for navigation.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: compositebucket.pipeline
spec:
  compositeTypeRef:
    apiVersion: platform.example.org/v1alpha1
    kind: CompositeBucket
  mode: Pipeline
  pipeline:
    - step: render-bucket
      functionRef:
        name: function-go-templating
